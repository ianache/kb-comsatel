# Keycloak (realm comsatel-km-prod per Portal KM Comsatel design)
KEYCLOAK_ISSUER=https://keycloak.comsatel.internal/realms/comsatel-km-prod
KEYCLOAK_CLIENT_ID=portal-km-bff
# Confidential client secret lives in Vault in real environments; only used for local/dev.
KEYCLOAK_CLIENT_SECRET=
BFF_BASE_URL=http://localhost:3000

# Session cookie signing key (32+ random bytes, base64). Rotate per environment.
SESSION_SECRET=

# Upstream FastAPI microservices
INGESTION_API_URL=http://localhost:8001
