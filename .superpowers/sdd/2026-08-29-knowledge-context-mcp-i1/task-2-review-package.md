# Task 2 review package

This repository has no commits. Review the current working-tree changes for Task 2 against the brief and report.

Files in scope:

- `src/domain/schemas.ts`
- `src/domain/errors.ts`
- `tests/domain/schemas.test.ts`

Inspect the files directly. Review read-only; do not mutate the workspace, index, HEAD, or branch.
